<div id="page-content">
  <div class="container">
    <div class="card mt-4 mb-4", id=<%= image_id %>>
      <div class="card-header">
        <h1 class="text-center mt-4", id="form-title"><%= title %></h1>
      </div>
      <div class="card-body">
        <%= render "/shared/errors", error_target: @coffeecard %>

        <%= form_with(model: @coffeecard, local: true) do |f| %>

        <div class="form-group row">
          <%= f.label t('form.title_label'), class: "col-2 col-form-label text-right" %>
          <div class="col-10">
            <%= f.text_field :title, class: "form-control", placeholder: t('form.title_placeholder'),
             maxlength: get_maxlength(Coffeecard, :title) %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label t('form.text_lang_label'), class: "col-2 col-form-label text-right" %>
          <div class="col-10">
            <% I18n.available_locales.each do |a| %>
            <div class="form-check form-check-inline pt-1">
              <%= f.radio_button :text_lang, a.to_s, class: "form-check-input" %>
              <label class="signup-label p-1" for="inlineRadio1"><%= I18n.t('language_name', locale: a) %></label>
            </div>
            <% end %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label t('form.description_label'), class: "col-2 col-form-label text-right" %>
          <div class="col-10">
            <%= f.text_area :description, class: "form-control", id: "exampleFormControlTextarea1", rows:"7",
             placeholder: t('form.description_lang_placeholder'), maxlength: get_maxlength(Coffeecard, :description)  %>
          </div>
        </div>

        <p class="text-center"><%= f.submit t('submit_button'), class: "btn btn-secondary font-weight-bold" %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>